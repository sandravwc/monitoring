#!/data/data/com.termux/files/usr/bin/sh
exec 2>&1
export SSL_CERT_FILE=$PREFIX/etc/tls/cert.pem   # Go looks in /etc/ssl, Termux keeps the CA bundle here
exec $HOME/monitoring/bin/blackbox_exporter --config.file=$HOME/monitoring/repo/deploy/blackbox.yml --web.listen-address=127.0.0.1:9115
